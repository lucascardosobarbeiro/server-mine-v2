### 2. terraform/docker-compose.yml (corrigido)

networks:
  minecraft-net:
    driver: bridge

services:
  mc-sobrevivencia:
    image: itzg/minecraft-server:java21
    container_name: mc-sobrevivencia
    restart: unless-stopped
    ports:
      - "25566:25565"
    volumes:
      - ./sobrevivencia-data:/data
      - ./paper/configs/paper-global.yml:/config/paper-global.yml
      - ./paper/configs/server.properties:/config/server.properties
      - ./velocity/forwarding.secret:/velocity/forwarding.secret
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      MEMORY: "10G"
      ONLINE_MODE: "false"
      VELOCITY_ENABLED: "true"
      VELOCITY_SECRET_FILE: "/velocity/forwarding.secret"
    networks:
      - minecraft-net
    depends_on:
      - velocity-proxy

  velocity-proxy:
    image: itzg/mc-proxy:latest
    container_name: velocity-proxy
    restart: unless-stopped
    ports:
      - "25565:25577"
    volumes:
      - ./velocity:/config
    environment:
      MEMORY: "1G"
    networks:
      - minecraft-net
