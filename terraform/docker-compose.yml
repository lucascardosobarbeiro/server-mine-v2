# docker-compose.yml

networks:
  minecraft-net:
    driver: bridge

services:
  mc-sobrevivencia:
    image: itzg/minecraft-server
    container_name: mc-sobrevivencia
    restart: unless-stopped
    ports:
      - "25566:25565"  # Porta interna continua 25565, mas exposta na 25566 para não conflitar com o Velocity
    volumes:
      - ./sobrevivencia-data:/data
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      MEMORY: "10G"
      ONLINE_MODE: "true"
      VELOCITY_ENABLED: "true"
      VELOCITY_SECRET_FILE: "/velocity/forwarding.secret"
    networks:
      - minecraft-net
    depends_on:
      - velocity-proxy

  velocity-proxy:
    image: itzg/velocity
    container_name: velocity-proxy
    restart: unless-stopped
    ports:
      - "25565:25577"  # Porta principal do proxy
    volumes:
      - ./velocity:/config
    environment:
      MEMORY: "1G"
    networks:
      - minecraft-net
